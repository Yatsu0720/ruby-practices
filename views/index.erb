<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="/style.css" />
  <title>Top -memo</title>
</head>

<body>
  <header></header>
  <div>
    <ul> 
     <% @json_data.each do |key, value| %>
          <%= '<li>' %>
          <%= '<a href='+"http://localhost:4567/memo/#{key}>#{value.keys[0]}</a>" %>
          <%= '</li>' %>
     <% end %>    
    </ul>
   
  </div>
</body>
</html>
